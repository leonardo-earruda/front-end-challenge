import { IsAvailableInBrazilPipe } from './yes-no.pipe';

describe('IsAvailableInBrazilPipe', () => {
  it('create an instance', () => {
    const pipe = new IsAvailableInBrazilPipe();
    expect(pipe).toBeTruthy();
  });
});
