import { MsToMinSecPipe } from './ms-to-min-sec.pipe';

describe('MsToMinSecPipe', () => {
  it('create an instance', () => {
    const pipe = new MsToMinSecPipe();
    expect(pipe).toBeTruthy();
  });
});
